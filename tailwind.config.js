/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './public/**/*.html',  // Includes all HTML files in public folder
    './public/**/*.js',    // Includes all JS files in public folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

